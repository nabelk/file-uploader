<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>KOTAK | Sign Up</title>
    <link rel="stylesheet" href="css/output.css" />
  </head>
  <body>
    <%- include('header') %>
    <main class="flex justify-center items-center h-full">
      <form class="max-w-sm mx-auto" method="POST" action="/signup">
        <h3 class="text-lg font-semibold text-gray-900 mb-4">
          Sign up to our KOTAK
        </h3>
        <div class="mb-5">
          <label
            for="firstName"
            class="block mb-2 text-sm font-medium text-gray-900"
            >First name</label
          >
          <input
            type="text"
            id="firstName"
            name="firstName"
            value="<%= formData.firstName || '' %>"
            class="shadow-sm bg-gray-50 border border-gray-300 text-gray-900 text-sm rounded-lg focus:ring-sky-500 focus:border-sky-500 block w-full p-2.5"
            placeholder="John"
            required
          />
          <%- include('partials/errormessages', { errorMessages, field:
          'firstName' }) %>
        </div>
        <div class="mb-5">
          <label
            for="lastName"
            class="block mb-2 text-sm font-medium text-gray-900"
            >Last name</label
          >
          <input
            type="text"
            id="lastName"
            name="lastName"
            value="<%= formData.lastName || '' %>"
            class="shadow-sm bg-gray-50 border border-gray-300 text-gray-900 text-sm rounded-lg focus:ring-sky-500 focus:border-sky-500 block w-full p-2.5"
            placeholder="Doe"
            required
          />
          <%- include('partials/errormessages', { errorMessages, field:
          'lastName' }) %>
        </div>
        <div class="mb-5">
          <label
            for="email"
            class="block mb-2 text-sm font-medium text-gray-900"
            >Email</label
          >
          <input
            type="email"
            id="email"
            name="email"
            value="<%= formData.email || '' %>"
            class="shadow-sm bg-gray-50 border border-gray-300 text-gray-900 text-sm rounded-lg focus:ring-sky-500 focus:border-sky-500 block w-full p-2.5"
            placeholder="johndoe@test.com"
            required
          />
          <%- include('partials/errormessages', { errorMessages, field: 'email'
          }) %>
        </div>
        <div class="mb-5">
          <label
            for="password"
            class="block mb-2 text-sm font-medium text-gray-900"
            >Password</label
          >
          <input
            type="password"
            id="password"
            name="password"
            class="shadow-sm bg-gray-50 border border-gray-300 text-gray-900 text-sm rounded-lg focus:ring-sky-500 focus:border-sky-500 block w-full p-2.5"
            required
          />
          <%- include('partials/errormessages', { errorMessages, field:
          'password' }) %>
        </div>
        <div class="mb-5">
          <label
            for="confirmPassword"
            class="block mb-2 text-sm font-medium text-gray-900"
            >Confirm password</label
          >
          <input
            type="password"
            id="confirmPassword"
            name="confirmPassword"
            class="shadow-sm bg-gray-50 border border-gray-300 text-gray-900 text-sm rounded-lg focus:ring-sky-500 focus:border-sky-500 block w-full p-2.5"
            required
          />
          <%- include('partials/errormessages', { errorMessages, field:
          'confirmPassword' }) %>
        </div>

        <p class="text-center text-sm text-gray-600 mb-4">
          <span>Already have an account?</span>
          <a href="/" class="text-sky-500 cursor-pointer">Login here</a>
        </p>

        <button
          type="submit"
          class="text-white bg-sky-700 hover:bg-sky-800 focus:ring-4 focus:outline-none focus:ring-sky-300 font-medium rounded-lg text-sm px-5 py-2.5 text-center"
        >
          Create a new account
        </button>
      </form>
    </main>
  </body>
</html>
